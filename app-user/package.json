{
    "scripts": {
        "start": "npm run dev",
        "dev": "source ~/.nvm/nvm.sh && nvm use 18 && vite",
        "build": "source ~/.nvm/nvm.sh && nvm use 18 && vite build"
    },
    "devDependencies": {
        "vite": "^6.0.11"
    }
}
